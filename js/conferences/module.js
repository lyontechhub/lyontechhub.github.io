define(['angular',
    'angularRoute',
    'angularSanitize'], function(angular) {
    'use strict';

    var app = angular.module("lyontechhub.conferences", ["ngRoute", 'ngSanitize']);

    return app;
});
